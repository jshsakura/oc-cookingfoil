# Source control
.git
.gitignore
.gitattributes

# Local env / IDE
.env
.env.*
!.env.example
.vscode
*.code-workspace

# Node / build artifacts
node_modules
npm-debug.log*
*.log
coverage
.nyc_output
*.tsbuildinfo

# CI / docs / diagrams (not needed at runtime)
.github
.diagrams
*.md
!shop_template.jsonc

# Tests
test
playwright-report
playwright.config.ts

# Local game library — must be mounted, never baked into the image
games

# Docker compose itself shouldn't be inside the image
Dockerfile
docker-compose*.yml
.dockerignore
